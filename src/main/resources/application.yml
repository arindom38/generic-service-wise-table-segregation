server:
  port: 8021
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/rpp_test
    username: rpp
    password: rpp
    hikari:
      data-source-properties:
        stringtype: unspecified
  jpa:
    properties.hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create
